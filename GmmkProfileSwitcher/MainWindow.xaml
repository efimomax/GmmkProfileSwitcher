<Window x:Class="GmmkProfileSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="GMMK Profile Switcher Settings" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="General Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        
        <CheckBox x:Name="chkStartWithWindows" Grid.Row="1" Content="Start with Windows (Автозагрузка)" Margin="0,0,0,5" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
        <CheckBox x:Name="chkStartMinimized" Grid.Row="2" Content="Start minimized to tray (Запускать свернутым)" Margin="0,0,0,15" Checked="Setting_Changed" Unchecked="Setting_Changed"/>

        <GroupBox Grid.Row="3" Header="Language to Profile Mapping">
            <ListView x:Name="lvLanguages" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding LanguageName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <ComboBox Grid.Column="1" SelectedItem="{Binding SelectedProfile}" SelectionChanged="Profile_SelectionChanged" Width="100" HorizontalAlignment="Left">
                                <ComboBox.Items>
                                    <sys:Int32>1</sys:Int32>
                                    <sys:Int32>2</sys:Int32>
                                    <sys:Int32>3</sys:Int32>
                                </ComboBox.Items>
                            </ComboBox>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </GroupBox>

        <Button Grid.Row="4" Content="Save &amp; Close" Width="100" Height="25" HorizontalAlignment="Right" Margin="0,10,0,0" Click="BtnSave_Click"/>
    </Grid>
</Window>
